# TODO

- [x] Update dependencies (check `package.json` for outdated packages, upgrade)
- [x] Review key frameworks (Next.js 15.5.9 and other core deps) for breaking changes, deprecations, or new conventions worth knowing about
- [x] Add a real integration test that reads from and replaces files in a Google Drive folder, using a dedicated test Drive folder (never the real photos/roster folders) — `src/__tests__/integration/photo-drive.integration.test.ts`, against "Photos Test - Fall 2026" and "Photo Carryover Test - Fall 2026"
- [x] Extend `/api/diagnostics` to also check the photo-upload OAuth identity (`GOOGLE_OAUTH_CLIENT_ID`/`SECRET`/`REFRESH_TOKEN`) and Drive/Sheets access for `PHOTOS_FOLDER_ID` and `FALL_2025_ROSTER_SHEET_ID` — it currently only checks the service account's Sheets/Drive access, not the separate OAuth identity the photo pipeline depends on
- [x] Push the 5 new integration-test secrets to GitHub Actions so the new Drive/Photo Carryover CI job actually runs instead of skipping itself: `PHOTOS_FOLDER_ID_TEST`, `FALL_2025_ROSTER_SHEET_ID_TEST`, `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `GOOGLE_OAUTH_REFRESH_TOKEN` (values already in `.env.local`; the `gh secret set` commands are ready, just need your go-ahead since it's a permission-gated action)
- [x] Shorten player ID length — `mintPlayerId` now defaults to 5 characters (was 10). Existing longer IDs stay valid; we never remint.
- [x] Make sure the `/signup` screen is consistent with the `/player` screen in terms of denoting what fields are required, etc. — shared `Req` / `HelperText` on both; signup now marks preferred name, last name, and DOB with the same red asterisk.
- [ ] Rotate `FINALFORMS_GITHUB_TOKEN` before it expires 2027-08-01 (classic PAT, `public_repo` scope, owned by the dedicated Madison Ultimate GitHub account; see `docs/fall-2026/signup-plan.md` section 6). Update both `.env.local` and the Vercel Sensitive env var; `/api/diagnostics` will confirm it's working again without triggering a real sync.
- [x] Give a stronger indication that a "Save" actually saved on the player profile form (`src/components/PlayerProfileForm.tsx`) — sticky bar turns the button green “✓ Saved” on success, and shows a red alert on failure; both stay until the next edit or retry.
- [ ] Decide the fate of the legacy Drive-CSV team-mailing-list feature (`TEAM_MAILING_LIST_FOLDER_ID`), now that Buttondown is the real subscription source of truth for `/signup`. It's threaded through `src/lib/roster-synthesizer.ts`, `src/lib/cache-manager.ts`, `src/lib/data-processing.ts`, `src/lib/types.ts` (`hasCaretaker1JoinedMailingList` etc., `MailingListRecord`), `src/components/SignupStatusTable.tsx`, and `src/app/player-portal/[portalId]/page.tsx` (mailing-status indicators/columns). Blocked on: is `/player-portal/[portalId]` still used this season, or fully superseded by `/signup` + `/player/$id` (it may get merged into `/player` instead)? Once that's settled, options are: (a) remove the env var + Drive-fetch/parse code only, leave the UI showing stale/"Unknown"; (b) also strip the UI columns that reference it; (c) rewire those same UI columns to live Buttondown status instead. `/api/diagnostics` already treats it as optional (warning, not fail) as of 2026-08-29. Note (2026-08-30): `src/components/SignupStatusTable.tsx` is now orphaned — its only consumer (`src/app/signup-report-hidden/page.tsx`, stale Fall 2025 copy) was deleted; the component itself is unused dead code pending this decision.
