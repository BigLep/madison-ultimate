// The portal's public address. Family-facing links written outside the browser (outreach
// emails, anything a script or route builds) always use this, never the host a request came
// in on, so a link is the same whether it was generated locally, on a preview, or in production.
export const PORTAL_PUBLIC_URL = 'https://madisonultimate.org';
