/**
 * Birth-year window for 6th–8th graders this season. The native date picker's
 * year dropdown is bounded by min/max, so families aren't scrolling from 2026
 * back through decades. Update these each season — see SEASON_SETUP.md.
 *
 * Fall 2026: same 2011–2015 window as the portal login year list.
 */
export const PLAYER_BIRTHDATE_MIN = '2011-01-01'
export const PLAYER_BIRTHDATE_MAX = '2015-12-31'
